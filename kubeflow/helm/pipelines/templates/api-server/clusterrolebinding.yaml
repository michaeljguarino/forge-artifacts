apiVersion: rbac.authorization.k8s.io/v1beta1
kind: ClusterRoleBinding
metadata:
  labels: {{- include "notebooks.labels" . | nindent 4 }}
  name: {{ include "notebooks.fullname" . }}-api-server-cluster-role-binding
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: {{ include "pipelines.fullname" . }}-api-server-cluster-role
subjects:
  - kind: ServiceAccount
    name: {{ include "pipelines.serviceAccountName" . }}-api-server
    namespace: kubeflow
