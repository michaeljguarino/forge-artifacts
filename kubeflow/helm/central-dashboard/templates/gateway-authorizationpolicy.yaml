{{- $fullName := include "central-dashboard.fullname" . -}}
apiVersion: security.istio.io/v1beta1
kind: AuthorizationPolicy
metadata:
  labels: {{- include "central-dashboard.labels" . | nindent 4 }}
  name: istio-ingressgateway-oidc
  namespace: {{ .Values.global.istioNamespace }}
spec:
  action: CUSTOM
  selector:
    # Same as the istio-ingressgateway Service selector
    matchLabels:
      app: istio-ingressgateway
      istio: ingressgateway
  provider:
    name: oauth2-proxy
  rules:
  - to:
    - operation:
        hosts:
        - {{ .Values.config.dashboardDomain }}
        # - <<__subdomain_kubecost__>>.<<__domain__>>
        # - <<__subdomain_serving__>>.<<__domain__>>
        # - '*.<<__subdomain_serving__>>.<<__domain__>>'
---
apiVersion: security.istio.io/v1beta1
kind: AuthorizationPolicy
metadata:
  name: {{ $fullName }}-allow-in-cluster-redirect
  namespace: {{ .Values.global.istioNamespace }}
spec:
  action: ALLOW
  selector:
    matchLabels:
      app: istio-ingressgateway
      istio: ingressgateway
  rules:
  - to:
    - operation:
        hosts:
        - {{ .Values.config.dashboardDomain }}   # needed for redirect after authentication
