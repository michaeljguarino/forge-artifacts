apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: {{ include "volumes.fullname" . }}-web-app
  namespace: kubeflow
  labels: {{- include "volumes.labels" . | nindent 4 }}
  {{- with .Values.virtualService.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  gateways:
    {{- range .Values.virtualService.gateways }}
    - {{ . | quote }}
    {{- end }}
  hosts:
    {{- range .Values.virtualService.hosts }}
    - {{ . | quote }}
    {{- end }}
  http:
    - headers:
        request:
          add:
            x-forwarded-prefix: {{ .Values.virtualService.prefix }}
      match:
        - uri:
            prefix: {{ .Values.virtualService.prefix }}/
      rewrite:
        uri: /
      route:
        - destination:
            host: {{ include "volumes.fullname" . }}-web-app.kubeflow.svc.{{ .Values.global.clusterDomain }}
            port:
              number: {{ .Values.service.port }}
