apiVersion: v1
kind: Service
metadata:
  name: {{ include "katib.fullname" . }}
  labels:
    {{- include "katib.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "katib.selectorLabels" . | nindent 4 }}
